import { AppPage } from './app.po';

describe('stacked App', () => {
  let page: AppPage;

  beforeEach(() => {
    page = new AppPage();
  });

  it('should expect true to be true', () => {
    expect(true).toBe(true);
  });
});
